{
  "name": "eraskon_inventory",
  "version": "1.0.0",
  "description": "",
  "main": "config.js",
  "scripts": {
    "start": "npm run build",
    "sync:web": "rsync -a assets/ www/assets/ && rsync -a components/ www/components/ && rsync -a controllers/ www/controllers/ && rsync -a models/ www/models/ && rsync -a resources/ www/resources/ && rsync -a *.html config.js capacitor.config.json sales-orders www/",
    "build": "npm run sync:web && npx cap sync android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ayaweisoft/eraskon_inventory.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Ayaweisoft/eraskon_inventory/issues"
  },
  "homepage": "https://github.com/Ayaweisoft/eraskon_inventory#readme",
  "dependencies": {
    "@capacitor/android": "^8.3.1",
    "@capacitor/cli": "^8.3.1",
    "@capacitor/core": "^8.3.1"
  }
}
